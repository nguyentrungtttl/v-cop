import {Text,SafeAreaView,View} from 'react-native'

export default function DoneW({navigation}){
    return(
        <SafeAreaView style={{alignItems:"center",justifyContent:"center", top:"50%"}}>
            <Text style={{fontSize:22}}>ĐÃ CHUYỂN TIỀN THÀNH CÔNG</Text>
        </SafeAreaView>
    )
}